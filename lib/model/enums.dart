// Enum for target muscle
enum TargetMuscle {
  abs,
  biceps,
  glutes,
  lats,
  quads,
  triceps,
  none,
  all,
}

// Enum for equipment
enum Equipment {
  barbell,
  bodyWeight,
  dumbbell,
  ezBarbell,
  kettlebell,
  smithMachine,
  all,
}
